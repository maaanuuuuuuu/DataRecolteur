'use strict';

describe('myApp.measure module', function() {

  beforeEach(module('myApp.measure'));

  describe('measure controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var measureCtrl = $controller('measureCtrl');
      expect(measureCtrl).toBeDefined();
    }));

  });
});